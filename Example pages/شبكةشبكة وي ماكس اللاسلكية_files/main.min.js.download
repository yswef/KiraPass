var $jscomp = $jscomp || {};
($jscomp.scope = {}),
($jscomp.createTemplateTagFirstArg = function (e) {
    return (e.raw = e);
}),
($jscomp.createTemplateTagFirstArgWithRaw = function (e, t) {
    return (e.raw = t), e;
}),
($jscomp.arrayIteratorImpl = function (e) {
    var t = 0;
    return function () {
    return t < e.length ? { done: !1, value: e[t++] } : { done: !0 };
    };
}),
($jscomp.arrayIterator = function (e) {
    return { next: $jscomp.arrayIteratorImpl(e) };
}),
($jscomp.makeIterator = function (e) {
    var t =
    "undefined" != typeof Symbol && Symbol.iterator && e[Symbol.iterator];
    return t ? t.call(e) : $jscomp.arrayIterator(e);
}),
($jscomp.arrayFromIterator = function (e) {
    for (var t, n = []; !(t = e.next()).done; ) n.push(t.value);
    return n;
}),
($jscomp.arrayFromIterable = function (e) {
    return e instanceof Array
    ? e
    : $jscomp.arrayFromIterator($jscomp.makeIterator(e));
}),
($jscomp.checkStringArgs = function (e, t, n) {
    if (null == e)
    throw new TypeError(
        "The 'this' value for String.prototype." +
        n +
        " must not be null or undefined"
    );
    if (t instanceof RegExp)
    throw new TypeError(
        "First argument to String.prototype." +
        n +
        " must not be a regular expression"
    );
    return e + "";
}),
($jscomp.ASSUME_ES5 = !1),
($jscomp.ASSUME_NO_NATIVE_MAP = !1),
($jscomp.ASSUME_NO_NATIVE_SET = !1),
($jscomp.SIMPLE_FROUND_POLYFILL = !1),
($jscomp.ISOLATE_POLYFILLS = !1),
($jscomp.FORCE_POLYFILL_PROMISE = !1),
($jscomp.FORCE_POLYFILL_PROMISE_WHEN_NO_UNHANDLED_REJECTION = !1),
($jscomp.defineProperty =
    $jscomp.ASSUME_ES5 || "function" == typeof Object.defineProperties
    ? Object.defineProperty
    : function (e, t, n) {
        return (
            e == Array.prototype || e == Object.prototype || (e[t] = n.value), e
        );
        }),
($jscomp.getGlobal = function (e) {
    e = [
    "object" == typeof globalThis && globalThis,
    e,
    "object" == typeof window && window,
    "object" == typeof self && self,
    "object" == typeof global && global,
    ];
    for (var t = 0; t < e.length; ++t) {
    var n = e[t];
    if (n && n.Math == Math) return n;
    }
    throw Error("Cannot find global object");
}),
($jscomp.global = $jscomp.getGlobal(this)),
($jscomp.IS_SYMBOL_NATIVE =
    "function" == typeof Symbol && "symbol" == typeof Symbol("x")),
($jscomp.TRUST_ES6_POLYFILLS =
    !$jscomp.ISOLATE_POLYFILLS || $jscomp.IS_SYMBOL_NATIVE),
($jscomp.polyfills = {}),
($jscomp.propertyToPolyfillSymbol = {}),
($jscomp.POLYFILL_PREFIX = "$jscp$");
var $jscomp$lookupPolyfilledValue = function (e, t) {
var n = $jscomp.propertyToPolyfillSymbol[t];
return null == n ? e[t] : void 0 !== (n = e[n]) ? n : e[t];
};
($jscomp.polyfill = function (e, t, n, o) {
t &&
    ($jscomp.ISOLATE_POLYFILLS
    ? $jscomp.polyfillIsolated(e, t, n, o)
    : $jscomp.polyfillUnisolated(e, t, n, o));
}),
($jscomp.polyfillUnisolated = function (e, t, n, o) {
    for (n = $jscomp.global, e = e.split("."), o = 0; o < e.length - 1; o++) {
    var r = e[o];
    if (!(r in n)) return;
    n = n[r];
    }
    (t = t((o = n[(e = e[e.length - 1])]))) != o &&
    null != t &&
    $jscomp.defineProperty(n, e, {
        configurable: !0,
        writable: !0,
        value: t,
    });
}),
($jscomp.polyfillIsolated = function (e, t, n, o) {
    var r = e.split(".");
    (e = 1 === r.length),
    (o = r[0]),
    (o = !e && o in $jscomp.polyfills ? $jscomp.polyfills : $jscomp.global);
    for (var i = 0; i < r.length - 1; i++) {
    var s = r[i];
    if (!(s in o)) return;
    o = o[s];
    }
    (r = r[r.length - 1]),
    null !=
        (t = t((n = $jscomp.IS_SYMBOL_NATIVE && "es6" === n ? o[r] : null))) &&
        (e
        ? $jscomp.defineProperty($jscomp.polyfills, r, {
            configurable: !0,
            writable: !0,
            value: t,
            })
        : t !== n &&
            (void 0 === $jscomp.propertyToPolyfillSymbol[r] &&
              ((n = (1e9 * Math.random()) >>> 0),
            ($jscomp.propertyToPolyfillSymbol[r] = $jscomp.IS_SYMBOL_NATIVE
                ? $jscomp.global.Symbol(r)
                : $jscomp.POLYFILL_PREFIX + n + "$" + r)),
            $jscomp.defineProperty(o, $jscomp.propertyToPolyfillSymbol[r], {
            configurable: !0,
            writable: !0,
            value: t,
            })));
}),
$jscomp.polyfill(
    "String.prototype.repeat",
    function (e) {
    return (
        e ||
        function (e) {
        var t = $jscomp.checkStringArgs(this, null, "repeat");
        if (0 > e || 1342177279 < e)
            throw new RangeError("Invalid count value");
        e |= 0;
        for (var n = ""; e; ) 1 & e && (n += t), (e >>>= 1) && (t += t);
        return n;
        }
    );
    },
    "es6",
    "es3"
),
$jscomp.polyfill(
    "Object.is",
    function (e) {
    return (
        e ||
        function (e, t) {
        return e === t ? 0 !== e || 1 / e == 1 / t : e != e && t != t;
        }
    );
    },
    "es6",
    "es3"
),
$jscomp.polyfill(
    "Array.prototype.includes",
    function (e) {
    return (
        e ||
        function (e, t) {
        var n = this;
        n instanceof String && (n = String(n));
        var o = n.length,
            r = t || 0;
        for (0 > r && (r = Math.max(r + o, 0)); r < o; r++) {
            var i = n[r];
            if (i === e || Object.is(i, e)) return !0;
        }
        return !1;
        }
    );
    },
    "es7",
    "es3"
),
$jscomp.polyfill(
    "String.prototype.includes",
    function (e) {
    return (
        e ||
        function (e, t) {
        return (
            -1 !==
            $jscomp.checkStringArgs(this, e, "includes").indexOf(e, t || 0)
        );
        }
    );
    },
    "es6",
    "es3"
);
var $loginData,
animating = !1,
submitPhase2 = 500,
logoutPhase1 = 20,
$login = document.getElementById("login"),
$status = document.getElementById("status"),
$loginSubmit = document.querySelector(".login-submit"),
loggedIn = !1,
intervalStatus = !1,
$passwordField = document.login.querySelector("input[password-field]"),
$advImage = document.querySelector("[hot-adv-img]"),
errorCount = 0,
$errorContainer = document.querySelector(".error-container");
function onLoginSubmitted() {
var e = document.getElementById("error");
1 > document.login.username.value.length
    ? ((e.innerText = "يجب ادخال قيمة صحيحة!"),
    showErrorPopup(),
    (animating = !1))
    : ((e.innerText = ""),
    $loginSubmit.classList.add("processing"),
    userLogin());
}
function onLogoutSubmitted(e, t) {
userLogout((e = e.hasAttribute("erase-cookie")));
}
function showParentElement(e, t) {
e && e.classList.add("success"),
    setTimeout(function () {
    (t.style.display = "block"),
        getComputedStyle(t).top,
        t.classList.add("active");
    }, submitPhase2),
    setTimeout(function () {
    $status.classList.contains("active") && t !== $status
        ? hideStatusPage()
        : hideLoginPage(),
        (animating = !1),
        e && (e.classList.remove("success"), e.classList.remove("processing"));
    }, submitPhase2 - 70);
}
function hideLoginPage() {
($login.style.display = "none"), $login.classList.add("inactive");
}
function hideStatusPage() {
($status.style.display = "none"), $status.classList.remove("active");
}
function showLoginPage() {
loggedIn
    ? showStatusPage()
    : (populateSelectWithOptions(),
    ($login.style.visibility = "visible"),
    ($login.style.display = "block"),
    getComputedStyle($login).top,
    $login.classList.remove("inactive"));
}
function showStatusPage() {
showParentElement(null, $status);
}
function initHotspotEvents(e, t) {
var n,
    o = t.querySelectorAll(".app-logout");
for (n in (e.addEventListener("click", function (e) {
    animating ||
    ((animating = !0),
    "status" === t.id
        ? onLoginSubmitted()
        : ("app-store" === t.id && loadScript("js/store.min.js"),
        this.classList.add("processing"),
        showParentElement(this, t)));
}),
o)) {
    var r = o[n];
    "object" == typeof r &&
    r.addEventListener("click", function (e) {
        if (!animating) {
        animating = !0;
        var n = this;
        "status" === t.id && onLogoutSubmitted(n, t),
            n.classList.add("clicked"),
            setTimeout(function () {
            t.classList.remove("active"), showLoginPage();
            }, submitPhase2 + 120),
            setTimeout(function () {
            (t.style.display = "none"),
                (animating = !1),
                n.classList.remove("clicked");
            }, submitPhase2);
        }
    });
}
}
function enc(e) {
var t = "";
const n = e.split(" ");
for (e = 0; e < n.length; e++) t += String.fromCharCode(parseInt(n[e]));
return t;
}
function toArabicSpeed(e) {
if ("" == e) return "سرعة متوسطة";
const t = document.getElementById("speed");
for (var n = 0; n < t.options.length; n++)
    if (t.options[n].value === e) return t.options[n].text;
return "";
}
function toArabicTime(e) {
var t = e,
    n = "",
    o = "",
    r = "",
    i = "";
return "" === e
    ? ""
    : (0 <= e.indexOf("d") &&
        ((n = "" !== (t = e.split("d"))[0] ? t[0] + " يوم " : ""), (t = t[1])),
    0 <= e.indexOf("h") &&
        ((o = "" !== (t = t.split("h"))[0] ? t[0] + " ساعة " : ""), (t = t[1])),
    0 <= e.indexOf("m") &&
        ((r = "" !== (t = t.split("m"))[0] ? t[0] + " دقيقة " : ""),
        (t = t[1])),
    0 <= e.indexOf("s") &&
        (i = "" !== (e = t.split("s"))[0] ? e[0] + " ثانية " : ""),
    n + o + r + i);
}
function toArabicBytes(e) {
return 1024 > e
    ? e + " بايت "
    : 1048576 > e
    ? Math.round(e / 1024) + " كيلوبايت "
    : Math.round(e / 1048576) + " ميجابايت ";
}
function hideHalfCard(e) {
if (0 <= (e = e.toLowerCase()).indexOf("T-")) return "تجربة مجانية";
if (0 <= e.indexOf(":")) return "اشتراك";
var t = Math.ceil(e.length / 2);
return (e = e.substring(0, t)) + "*".repeat(t);
}
function toArabicError(e) {
var t,
    n = {
    "user&not found":
        "أن هذا الحساب غير موجود أو قد انتهت صلاحية ، حاول مرة اخرى",
    "simultaneous session limit reached|no more sessions are allowed":
        "المعذرة ، هذا الكرت مستخدم حالياً في جهاز آخر",
    "invalid password": "تاكد من كتابة كلمة المرور بشكل صحيح",
    "uptime limit reached|No more online time|uptime limit":
        "عذراً لقد انتهى الوقت المتاح لك",
    "traffic limit|transfer limit reached": "لقد انتهى رصيد هذا الحساب",
    "invalid username or password|not found":
        "أسم المستخدم أو كلمة المرور غير صحيحة، الرجاء المحاولة مرة اخرى",
    "no valid profile found": "لقد انتهت صلاحية هذا الكرت",
    "invalid Calling-Station-Id": "هذا الحساب مقترن بجهاز آخر!",
    "server&is&not&responding":
        "هذا الحساب غير موجود, يرجى التأكد والمحاولة مرة اخرى",
    "web&browser&did&not&send": "يرجى محاولة ادخال الكرت مرة اخرى",
    "allowed to log in from this MAC":
        "لايحق لك استخدام هذا الكرت, الكرت محجوز لمستخدم اخر!",
    },
    o = "حصل خطأ: " + e,
    r = function (t) {
    return e.includes(t);
    };
for (t in n) {
    var i = n[t];
    if (t.includes("&")) {
    if (t.split("&").every(r)) {
        o = i;
        break;
    }
    } else if (t.includes("|")) {
    if (t.split("|").some(r)) {
        o = i;
        break;
    }
    } else if (e.includes(t)) {
    o = i;
    break;
    }
}
return o;
}
function onLoginStart(e) {
($loginData = e),
    ($login.style.visibility = "visible"),
    cookieLogin(),
    setTimeout(function () {
    return document.login.username.focus();
    }, 50);
}
function onLoginError(e) {
if (
    (document.login.reset(),
    setTimeout(function () {
    $loginSubmit.classList.remove("success"),
        $loginSubmit.classList.remove("processing");
    }, 500),
    (animating = !1),
    !e.error.includes("already authorizing, retry later"))
) {
    if (
    ((document.getElementById("error").innerText = toArabicError(e.error)),
    showErrorPopup(),
    1 === hotspotConfig["enable-hot-blocker"])
    ) {
    var t = incrementCounter(e);
    checkFailsCount(e, t);
    }
    clearCookies();
}
}
function onLoggedIn(e) {
(loggedIn = !0),
    remember(),
    showParentElement($loginSubmit, document.getElementById("status")),
    null != e && (resetCounter(), startStatusQuery());
}
function onLoggedOut(e) {
loggedIn = !1;
}
Function.prototype.appendFunction ||
(Function.prototype.appendFunction = function (e) {
    var t = this.name;
    "function" != typeof window[t] ||
    (window[t] = (function (t) {
        return function () {
        t.apply(null, arguments), e.apply(null, arguments);
        };
    })(window[t]));
}),
document.write(enc(" "));
var un = "";
function toArabicUpdate(e) {
return "Uoff" == e ? "تم  أيقافها" : "تم تفعيلها ";
}
function dsplit(e, t) {
if (e.length < 2) return "";
var n = e.split("_");
return t >= 0 && t < n.length ? n[t] : "";
}
function onStatusQuery(e) {
if (((un = e), e.logged_in))
    for (var t in e) {
    var n = e[t];
    if ("string" == typeof n && "logged_in" !== t) {
        var o = document.getElementById(t);
        null != o &&
        ("" === n
            ? (n = document.querySelector("[".concat(t, "]")))
            : "username" === t
            ? (o.innerText = hideHalfCard(n))
            : t.includes("bytes")
            ? (o.innerText = toArabicBytes(n))
            : t.includes("time")
            ? (o.innerText = toArabicTime(n))
            : t.includes("update")
            ? (o.innerText = toArabicUpdate(dsplit(n, 1)))
            : (t.includes("sspeed"),
            (o.innerText = toArabicSpeed(dsplit(n, 0)))));
    }
    }
else loggedIn = !1;
try {
    e.username.includes(e.mac.split("%3A").join(":")) &&
    ((t = "كرت مجاني من خدمة اجمع واربح!"),
    e.username.includes("T-") && (t = "انت الان تستخدم خدمة سلفني"),
    (document.getElementById("status-title").innerText = t));
} catch (e) {}
}
function getRequest(e) {
if ("file:" === window.location.protocol) showLoginPage();
else {
    var t = new XMLHttpRequest();
    t.open("GET", e, !0),
    (t.onreadystatechange = function () {
        if (4 === this.readyState) {
        var t = this.responseText;
        try {
            var n = JSON.parse(t);
            window[n.action](n);
        } catch (t) {
            "/status?var=callBack" === e &&
            ((loggedIn = 0),
            hideStatusPage(),
            showLoginPage(),
            cookieLogin());
        }
        }
    }),
    t.send();
}
}
function HLogin(e, t) {
getRequest(
    (e = "/login?username=".concat(e, "&password=").concat(t, "&var=callBack"))
);
}
var flag = 1,
f = !0;
function Ulogin(e, t, n) {
"" !== n && (n = "&domain=" + n),
    getRequest(
    (e = "/login?username="
        .concat(e, "&password=")
        .concat(t, "&var=callBack")) + n
    );
}
function userLogin() {
var e,
    t = document.login.querySelector("select[speed-field]"),
    n = document.getElementById("chupdate").value,
    o = t.options[t.selectedIndex].value,
    r = document.login.querySelector("input[username-field]").value,
    i =
    null != (e = document.login.querySelector("input[password-field]"))
        ? e
        : "";
"" !== i && (i = i.value), f && "" == o && (n = ""), Ulogin(r, i, o + n);
}
function a(e) {
for (var t = "", n = 0; n < e.length; n++) {
    var o = e.charCodeAt(n) + 3;
    t += String.fromCharCode(o);
}
return "[def456 ]" + t;
}
function b(e) {
if (e.startsWith("[def456 ]")) {
    var t = e.slice("[def456 ]".length),
    n = "";
    for (e = 0; e < t.length; e++) {
    var o = t.charCodeAt(e) - 3;
    n += String.fromCharCode(o);
    }
    return n;
}
return e;
}
function c(e) {
if (e.startsWith("54vb4cfghhghghdrtrytysfdfghbtrytyb5vb4v54b")) {
    var t = e.slice("54vb4cfghhghghdrtrytysfdfghbtrytyb5vb4v54b".length),
    n = "";
    for (e = 0; e < t.length; e++) {
    var o = t.charCodeAt(e);
    n += String.fromCharCode(o);
    }
    return n;
}
return e;
}
function userLogout() {
getRequest(
    0 < arguments.length && void 0 !== arguments[0] && arguments[0]
    ? "/logout?erase-cookie=yes&var=callBack"
    : "/logout?var=callBack"
);
}
function startStatusQuery() {
if (!intervalStatus && loggedIn) {
    (intervalStatus = !0), getRequest("/status?var=callBack");
    var e = setInterval(function () {
    loggedIn
        ? getRequest("/status?var=callBack")
        : ((intervalStatus = !1), clearInterval(e));
    }, 1e3);
}
}
function bytescalc(e) {
var t = "m056fd9fdfdsffsdffdfd1697455",
    n = e.indexOf(t) + t.length,
    o = e.lastIndexOf("dsfd6571fgfgfgfgdf53sdfdsfgsd14");
return e.substring(n, o);
}
function getDeviceType() {
var e = navigator.userAgent || navigator.vendor || window.opera,
    t = !1;
try {
    localStorage.setItem("test", "test"),
    localStorage.removeItem("test"),
    sessionStorage.setItem("test", "test"),
    sessionStorage.removeItem("test");
} catch (e) {
    t = !0;
}
return /windows phone/i.test(e)
    ? t
    ? "WindowsPhoneCaptive"
    : "WindowsPhone"
    : /android/i.test(e)
    ? t
    ? "AndroidCaptive"
    : "Android"
    : /iPad|iPhone|iPod/.test(e) && !window.MSStream
    ? !/safari/i.test(e) || t
    ? "ISOCaptive"
    : "IOS"
    : t
    ? "Captive"
    : "unknown";
}
function showErrorPopup() {
$errorContainer.classList.add("active"),
    setTimeout(function () {
    $errorContainer.classList.add("zoom");
    }, 10);
}
function hideErrorPopup() {
$errorContainer.classList.remove("active"),
    $errorContainer.classList.remove("zoom");
}
function calculateRemainingTime(e) {
    var t = e - new Date(),
        n = (Math.floor(t / 36e5), Math.floor((t % 36e5) / 6e4)),
        o = Math.floor((t % 6e4) / 1e3);
    return 0 != n ? n + " دقيقة " + o + " ثانية" : o + " ثانية";
}
document.getElementById("speedchange").addEventListener("change", function (e) {
if ("cancel" !== e.target.value) {
    var t = hotspotConfig["speed-trychange-timeout"],
    n = hotspotConfig["speed-trychange"],
    o = document.getElementById("error");
    null == getCookieOrNull("t") && setCookie("t", 1, t);
    var r = parseInt(getCookieOrNull("t")),
    i = bytescalc(un.bytesm);
    if ("" !== i)
    if (r <= n) {
        (o.innerHTML =
        "تم اختيار " +
        toArabicSpeed(e.target.value) +
        " <br><br> يتبقى لديك  " +
        (n - r) +
        " مرة لتغيير السرعة أو عد بعد  " +
        t +
        " دقائق "),
        setCookie("t", r + 1, t);
        var s = new Date(),
          a = new Date(s.getTime() + 60 * t * 1e3).toISOString();
        setCookie("ex", a, t + 2),
        getRequest("/logout"),
        Ulogin(i, "", e.target.value ),
        showErrorPopup(),
        setTimeout(function () {
            window.location.reload();
        }, 2500);
    } else
        (o.innerHTML =
        "يمكنك تغيير السرعة بعد  <br>" +
        calculateRemainingTime(new Date(getCookieOrNull("ex")))),
        showErrorPopup();
    else (o.innerText = "هذا الجهاز لا يدعم تغيير السرعة"), showErrorPopup();
}
else e.target.selectedIndex = 0;
}),
document
    .getElementById("updatechange")
    .addEventListener("change", function (e) {
    if ("cancel" !== e.target.value) {
        var t = document.getElementById("error");
        (i = bytescalc(un.bytesm)),
        (s = dsplit(un.sspeed, 0)),
        "" !== i
            ? ((t.innerHTML =
                "تم اختيار " +
                e.target.options[e.target.selectedIndex].innerText),
            getRequest("/logout"),
            Ulogin( "", s + e.target.value),
            showErrorPopup(),
            setTimeout(function () {
                window.location.reload();
            }, 2500))
            : ((t.innerText = "هذا المتصفح لا يدعم التغيير "),
            showErrorPopup());
    } else e.target.selectedIndex = 0;
    }),
document.addEventListener("DOMContentLoaded", function () {
    $errorContainer.addEventListener("click", hideErrorPopup);
    var e,
    t = document.querySelectorAll("button[parent-id]");
    for (e in t) {
    var n = t[e];
    if ("object" == typeof n) {
        var o;
        if (null === (o = t[e].getAttribute("parent-id"))) return;
        initHotspotEvents(n, (o = document.getElementById(o)));
    }
    }
    document.login.addEventListener("submit", function (e) {
    animating || onLoginSubmitted(), e.preventDefault();
    }),
    setTimeout(function () {
        getRequest("/login?var=callBack");
    }, 50);
}),
document.getElementById("chupdate").addEventListener("change", function (e) {
    var t = document.getElementById("error");
    (f = !1),
    e.target.checked
        ? ((t.innerText =
            " تم اختيار أيقاف المتجر و التحديثات *لن يعمل متجر التطبيقات و يمكنك تغيير ذلك لاحقا*"),
        (e.target.value = "_Uoff"))
        : ((t.innerText = "تم اختيار تفعيل المتجر و التحديثات "),
        (e.target.value = "_Uon")),
    showErrorPopup();
});
var msg = document.getElementById("winmsg").innerText;
function containsArabicText(e) {
return /[\u0600-\u06FF]/.test(e);
}
containsArabicText(msg) &&
((document.getElementById("error").innerText = msg), showErrorPopup());
const selectElement = document.getElementById("speed"),
options = Array.from(selectElement.options).slice(1),
newSelectElement = document.getElementById("speedchange");
options.forEach(function (e) {
const t = new Option(e.text, e.value);
newSelectElement.add(t);
}),
document.getElementById("speed").addEventListener("change", function (e) {
    (document.getElementById("error").innerText =
    "تم أختيار " + e.target.options[e.target.selectedIndex].innerText),
    showErrorPopup();
});
