# توثيق وتحليل أداة MikrotikBF

## 1. نظرة عامة (Overview)
هذا السكريبت (`layer_3.py`) عبارة عن أداة مكتوبة بلغة بايثون (Python) تهدف إلى القيام بعملية "تخمين" (Brute Force) على صفحات تسجيل الدخول الخاصة بشبكات "ميكروتيك" (Mikrotik Hotspot).
يقوم السكريبت بتوليد كلمات مرور وأسماء مستخدمين عشوائية بناءً على معايير يحددها المستخدم، ثم يقوم بإرسالها إلى رابط صفحة تسجيل الدخول للتأكد من صحتها.

## 2. آلية عمل الكود (How it Works)
1. **التهيئة:** يبدأ البرنامج بتنظيف الشاشة وإظهار واجهة رسومية بسيطة عبر التيرمنال باستخدام ألوان مختلفة.
2. **أخذ المدخلات:**
   - يطلب من المستخدم إدخال رابط صفحة تسجيل الدخول (مثل: `http://10.0.0.1/login`).
   - يقوم باختبار الاتصال المبدئي بالرابط للتأكد من عمله.
   - يطلب نوع الشبكة: 
     1. اسم مستخدم فقط.
     2. اسم مستخدم وكلمة مرور مخفية (حيث تكون كلمة المرور مطابقة لاسم المستخدم التطابق التام).
     3. اسم مستخدم وكلمة مرور منفصلين.
   - يأخذ تفضيلات الحسابات المراد تخمينها (نوع الحروف والأرقام، طول الكلمة، البادئة، والنهاية).
   - يطلب عدد المحاولات (عدد الحسابات المراد تخمينها).
   - يسأل عن الكلمة المفتاحية (Keyword) التي تظهر في الصفحة عند نجاح تسجيل الدخول (الافتراضي هو: `status`).
3. **عملية التخمين:**
   - يبدأ حلقة تكرار (Loop) بعدد المحاولات المحددة.
   - يتم بناء الحسابات العشوائية بواسطة مكتبة `random.sample` بحسب الطول والشروط المدخلة.
   - يتم إرسال طلب `GET` يحتوي على الحسابات في مسار الرابط (URL parameters) كالتالي:
     - `?username=XXX` أو `?username=XXX&password=YYY`.
   - يُؤخِّر الإرسال لمدة نصف ثانية `sleep(0.5)` في كل دورة لمنع الحظر.
   - يتم فحص نص الاستجابة `r.text` للبحث عن الكلمة المفتاحية (مثل `status`). إذا وُجدت، يُعلن نجاح العملية ويتوقف البرنامج.

---

## 3. الأخطاء والمشاكل البرمجية والمنطقية (Bugs & Errors)

1. **خطأ قاتل في توليد العشوائية (ValueError in `random.sample`):**
   - الكود يستخدم الدالة `sample(characters, length)` لتوليد النصوص العشوائية. مشكلة هذه الدالة أنها تسحب عينات **بدون تكرار**. إذا كان طول السلسلة المطلوبة أكبر من عدد الحروف/الأرقام المتاحة (مثلاً أرقام فقط: 10 خيارات، والطول المطلوب: 15)، ستتوقف الأداة وتُصدر خطأ.
   - حاول المبرمج ترقيع المشكلة في السطر `84` بإضافة `characters = characters + characters`، ولكن هذا الترقيع فاشل لأن تكرار الحروف مرة واحدة لا يكفي إذا كان الطول كبيراً، كما أنه تناسى إضافة هذا الترقيع المتواضع في الحالة الثالثة (`network_type == '3'`).

2. **طريقة الإرسال (GET vs POST):**
   - السكريبت يستخدم دائماً `requests.get()` لتسجيل الدخول ويمرر البيانات في الرابط. العديد من شبكات ميكروتيك لا تقبل الـ `GET` لأسباب أمنية وتتطلب `POST`، بالإضافة إلى استخدام نظام التشفير `chap-id` لـ MD5 لمزيد من الأمان.
   
3. **مشكلة الأداء (البطء الشديد):**
   - السكريبت يعمل بمسار واحد (Single-threaded) ويحتوي على انتظار إجباري: `sleep(0.5)` في كل دورة. هذا يعني أن إرسال 1000 محاولة سيأخذ 500 ثانية (حوالي 8-9 دقائق)، وهو بطيء جداً بالنسبة لأداة تخمين (Brute Force).

4. **تداخل الواجهة (UI Glitch):**
   - شريط التقدم (`progress_bar`) يعتمد على ميزة العودة للخلف `\r`، ولكن في كل محاولة يقوم البرنامج بطباعة الكود المولد `print(purple+last_char)` مما يؤدي إلى تشوه شريط التقدم في واجهة سطر الأوامر (Terminal) وظهور خطوط متعددة عشوائية بلاً من تحديث نفس السطر.

5. **فحص الاتصال الضعيف:**
   - السكريبت يعتبر أي رد ليس `200 OK` بأنه خطأ في الاتصال ويقوم بكسر الحلقة `break` (وهذا خاطئ، حيث أن ميكروتيك أحياناً تعيد `302 Found` لإعادة التوجيه إلى صفحة الـ Status في حال نجاح الدخول).

---

## 4. التحسينات المقترحة (Suggested Improvements)

1. **استبدال `random.sample` بـ `random.choices`:**
   - هذا هو الحل الصحيح لتوليد النصوص مع إمكانية التكرار دون مشاكل مع الطول:
     `rand_chars = "".join(random.choices(characters, k=length))`
     
2. **استخدام تعدد المهام (Threading / Asyncio):**
   - لتحقيق أداء عالي وسريع، يجب تحويل آلية الفحص للعمل بالتوازي بحيث تقوم الأداة بتجربة 10-50 حساب في الثانية الواحدة بدلاً من حساب واحد كل نصف ثانية.

3. **دعم طلبات POST وإدارة الجلسات (Sessions):**
   - يجب السماح بإرسال طلبات الـ POST والتأقلم مع تشفير شبكات ميكروتيك الحديثة (مثل إرسال parameters مثل: `dst`, `popup`, `username`, `password`).
   - يُفضل استخدام `requests.Session()` للحفاظ على الكوكيز (Cookies) في حال لزم الأمر.
   
4. **إضافة ترويسات متصفح (User-Agent):**
   - لجعل الطلب يبدو وكأنه صادر من متصفح شرعي، يجب تمرير `Headers` في طلب `requests` لتجنب حظر الأداة بواسطة أنظمة الحماية على أجهزة الشبكة.

5. **إصلاح شريط التقدم وفرز الطباعة:**
   - يجب عدم طباعة التجربة الحالية طالما أننا نستخدم شريط تقدم إلا إذا كانت النتيجة صحيحة. وإذا أُريد طباعة الحسابات أثناء الفحص، يمكن استخدام مكتبات مثل `tqdm` أو مكتبة `rich` التي تتعامل مع التحديثات الحية بسلاسة بدون تشوه الأسطر.

6. **تحسين تقييم نجاح تسجيل الدخول:**
   - عدم الاعتماد فقط على `200 status_code`. في شبكات ميكروتيك، النجاح غالبا يتمثل في الحصول على إعادة توجيه لكلمة `status`، أو في تغيير رابط المتصفح أو الرسالة العائدة بالـ HTML. 
   - يمكن إضافة فحص لمحتوى `r.url` بحيث إذا كان يحتوي على `status` يُعتبر الدخول ناجحاً دون الاعتماد الكلي على كلمة مفتاحية داخل الرد.
